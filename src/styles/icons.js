import { Box } from "@mui/system";
import { styled } from "@mui/material/styles";
export const Box__Icon = styled(Box)(() => ({
  display: "flex",
  padding: "50px",
  flexDirection: "row",
  justifyContent: "space-evenly",
}));
